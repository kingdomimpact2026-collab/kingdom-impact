const API_BASE="https://YOUR-BACKEND-URL.example.com";
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');if(menu)menu.onclick=()=>nav.classList.toggle('open');
document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));document.getElementById('year').textContent=new Date().getFullYear();
const form=document.getElementById('contactForm'),status=document.getElementById('status'),button=document.getElementById('submit');form?.addEventListener('submit',async e=>{e.preventDefault();button.disabled=true;button.textContent='Sending...';status.className='';try{const r=await fetch(API_BASE+'/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const d=await r.json();if(!r.ok)throw new Error(d.message||'Unable to send request.');status.className='show ok';status.textContent='Thank you. Your request has been submitted successfully.';form.reset()}catch(err){status.className='show err';status.textContent=err.message||'Could not submit. Please use WhatsApp or phone.'}finally{button.disabled=false;button.textContent='Submit Request'}});
