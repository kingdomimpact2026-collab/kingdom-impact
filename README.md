# Kingdom Impact Technologies Inc. — Professional Installable PWA

Upload all files in this folder to the existing GitHub Pages repository, replacing the matching files.
Keep `index.html`, `manifest.json`, `service-worker.js`, and `kingdom-impact-logo.jpg` in the repository root.
The site is a Progressive Web App and uses WhatsApp for client requests, so no paid backend is required.
