# Kingdom Impact Technologies Inc. — Installable Web App
This package is a Progressive Web App (PWA). Host it over HTTPS (GitHub Pages works) and Android users can install it from the browser when supported. The client request form still uses WhatsApp; no paid backend is included.
